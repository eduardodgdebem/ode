fn fib(num: number): number {
  if(num == 0 || num == 1) {
    return num;
  }

  return fib(num-1) + fib(num-2);
}

fn main(): number {
  let local_num: number = 9;
  let result = fib(local_num);
  print(result);
}
