fn fib(num: i32): i32 {
  if(num == 0 || num == 1) {
    return num;
  }

  return fib(num-1) + fib(num-2);
}

fn main(): i32 {
  let local_num: i32 = 9;
  let result: i32 = fib(local_num);
  print(result);
}
